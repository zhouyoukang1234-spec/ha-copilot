# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.ruff_cache/

# HA-Copilot backups
*.copilot.bak

# Local HA test instance (never commit a real config / secrets)
test-config/
*.log
.storage/
secrets.yaml

# hactl operator token (long-lived access token; never commit)
.ha_token

# OS / editor
.DS_Store
.idea/
.vscode/
